/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  env: {
    // Ensure these are available if needed, though process.env is preferred
  }
};

export default nextConfig;